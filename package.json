{
  "name": "sample-app",
  "version": "1.1.0",
  "description": "A minimal spa setup using typescript, preact, and parceljs",
  "main": "index.js",
  "private": true,
  "scripts": {
    "start": "npx parcel src/index.html",
    "build": "npx parcel build src/index.html",
    "release": "npx standard-version",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "preact": "10.0.0-rc.1",
    "preact-router": "^3.0.1"
  },
  "devDependencies": {
    "@types/node": "^12.7.1",
    "parcel-bundler": "^1.12.3",
    "standard-version": "^7.0.0",
    "typescript": "^3.5.3"
  }
}
